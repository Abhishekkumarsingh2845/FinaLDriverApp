//Interface are the used to define the structure the of the object and specify the property and method of the object has to do

interface User {
  id: number;
  name: string;
}

const data = (User = { id: 1, name: "abhishek" });
